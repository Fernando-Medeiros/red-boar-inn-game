export type Offensive = {
  name: string;
  type: string;
  attack: number;
  magic: number;
  description?: string;
};

export type Defensive = {
  name: string;
  type: string;
  defense: number;
  resistance: number;
  description?: string;
};
